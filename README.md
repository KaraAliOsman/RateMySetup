# RateMySetup

Aplicacion web simple hecha con Django para compartir setups de escritorio.

## Ejecutar

```bash
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver
```

Despues abre `http://127.0.0.1:8000/`.
